name: build

on:
  push:
  pull_request:

jobs:
  macos:
    name: swift build (macOS)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
